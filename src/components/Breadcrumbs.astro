---
import { defaultLang, localizePath, type Lang } from '../i18n';
import { useTranslations } from '../i18n/ui';

export interface Crumb {
  href?: string;
  label: string;
}

export interface Props {
  crumbs: Crumb[];
  lang?: Lang;
}

const { crumbs, lang = defaultLang } = Astro.props;
const t = useTranslations(lang);
const trail: Crumb[] = [{ href: localizePath('/', lang), label: t('crumb.home') }, ...crumbs];
---

<nav class="breadcrumbs" aria-label={t('crumb.label')}>
  <div class="shell">
    <ol>
      {
        trail.map((crumb, index) => {
          const isLast = index === trail.length - 1;
          return (
            <li>
              {crumb.href && !isLast ? (
                <a href={crumb.href}>{crumb.label}</a>
              ) : (
                <span aria-current="page">{crumb.label}</span>
              )}
              {!isLast && (
                <span class="sep" aria-hidden="true">
                  /
                </span>
              )}
            </li>
          );
        })
      }
    </ol>
  </div>
</nav>

<style>
  .breadcrumbs {
    border-bottom: 1px solid var(--line);
    background: var(--sand-100);
    font-size: 0.86rem;
  }
  ol {
    list-style: none;
    margin: 0;
    padding: 0.7rem 0;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.35rem;
  }
  li { display: flex; align-items: center; gap: 0.35rem; }
  a { color: var(--ink-soft); text-decoration: none; }
  a:hover { color: var(--ink); text-decoration: underline; }
  [aria-current='page'] { color: var(--ink); font-weight: 600; }
  .sep { color: var(--ink-faint); }
</style>
